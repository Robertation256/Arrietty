server:
  port: 8000

spring:
  datasource:
    username: root
    password: 123
    url: jdbc:mysql://localhost:3306/arrietty_fat_db?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    hostname: 127.0.0.1
    port: 6379
    database: 0
    password:
    lettuce:
      max-active: 8
      max-wait: -1
      max-idle: 8
      min-idle: 0
    timeout: 5000
  resources:
    static-locations: file:${file.image-path}, classpath:/static/

mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.arrietty.entity



logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n"
  file: log/info.log
  level:
    com:
      arrietty:
        info

file:
  image-path: D:/Users/yuechuanzhang/image/
  avatar-image-path: D:/Users/yuechuanzhang/image/avatars